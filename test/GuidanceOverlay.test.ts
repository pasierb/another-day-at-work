import assert from'node:assert/strict';import{test}from'node:test';import{GUIDANCE_COPY}from'../src/game/presentation/GuidanceContent.ts';test('guidance describes sticky-only play',()=>{const all=Object.values(GUIDANCE_COPY).join(' ');assert.match(all,/sticky/i);assert.match(all,/case/i);assert.doesNotMatch(all,/Prompt mode|Plan mode|approve the batch|Focus/i);assert.match(all,/Stamina/);});
