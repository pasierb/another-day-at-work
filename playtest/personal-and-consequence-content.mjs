export default [
 {name:'30-event catalog with new categories is wired',action:'expect',expect:{expression:`(()=>{const d=scene('Workstation').scheduler.definitions;return d.length===30&&d.filter(x=>x.category==='personal').length===4&&d.filter(x=>x.category==='consequence').length===5;})()`,equals:true}},
 {name:'sleep and toilet threshold warnings activate',action:'eval',code:`const s=scene('Workstation');s.scheduler.nextSpawnGameMs=null;s.playerNeeds.mutateNeed('sleepiness',100);s.playerNeeds.mutateNeed('toilet',100);s.synchronizeWorld();s.interruptions.activate('personal-sleep-warning',s.workday.snapshot.elapsedGameMs,1);s.interruptions.activate('personal-bathroom-warning',s.workday.snapshot.elapsedGameMs,2);window.__warningIds=s.interruptions.snapshot.active.map(x=>x.id);`},
 {name:'both authoritative warnings are visible',action:'expect',expect:{expression:`window.__warningIds.includes('personal-sleep-warning')&&window.__warningIds.includes('personal-bathroom-warning')&&scene('Workstation').alertsContent.getAll().some(x=>x.text==='PERSONAL')`,equals:true}},
 {name:'personal-warning-thresholds',action:'screenshot'},
 {name:'bathroom relief suppresses stale toilet warning',action:'eval',code:`const s=scene('Workstation');s.visitBathroom();window.__relief={stage:s.playerNeeds.snapshot.toilet.stage.id,active:s.interruptions.snapshot.active.map(x=>x.id)};`},
 {name:'relief removes only matching warning',action:'expect',expect:{expression:`window.__relief.stage==='could-wait'&&!window.__relief.active.includes('personal-bathroom-warning')&&window.__relief.active.includes('personal-sleep-warning')`,equals:true}},
 {name:'resolving warning remains exact once during relief',action:'eval',code:`const s=scene('Workstation');s.interruptions.reset();s.playerNeeds.mutateNeed('toilet',100);s.interruptions.activate('personal-bathroom-warning',s.workday.snapshot.elapsedGameMs,3);s.openDecision('personal-bathroom-warning');const pending=s.interruptions.beginResolution('go-now');s.playerNeeds.relieve('toilet');s.interruptions.dismissStale(s.interruptionContext());const kept=s.interruptions.snapshot.active.length;s.interruptions.finalizeResolution(pending.interruptionId);window.__resolving={kept,count:s.interruptions.snapshot.resolvedOutcomes.length,again:s.interruptions.finalizeResolution(pending.interruptionId)};s.workday.resume('interruption-decision');`},
 {name:'resolving relief preserves exact-once outcome',action:'expect',expect:{expression:`window.__resolving.kept===1&&window.__resolving.count===1&&!window.__resolving.again`,equals:true}},
 {name:'repeated Coffee exposes a traceable crash',action:'eval',code:`const s=scene('Workstation');s.boosters.counts.coffee=3;s.interruptions.activate('consequence-coffee-crash',s.workday.snapshot.elapsedGameMs,4);s.openDecision('consequence-coffee-crash');window.__coffee=s.interruptions.snapshot.openInterruption;`},
 {name:'coffee crash has current-state provenance',action:'expect',expect:{expression:`window.__coffee.category==='consequence'&&window.__coffee.cause.type==='current-state'&&window.__coffee.copy.includes('repayment')`,equals:true}},
 {name:'resolve coffee crash',action:'eval',code:`scene('Workstation').resolveChoice('hydrate-and-reset');`},
 {name:'shortcut choice enables traceable regression',action:'eval',code:`const s=scene('Workstation');s.interruptions.activate('po-scope-request',s.workday.snapshot.elapsedGameMs,5);s.openDecision('po-scope-request');s.resolveChoice('accept-scope');s.interruptions.activate('consequence-scope-regression',s.workday.snapshot.elapsedGameMs,6);s.openDecision('consequence-scope-regression');window.__regression=s.interruptions.snapshot.openInterruption;`},
 {name:'regression points to source choice without hidden copy',action:'expect',expect:{expression:`window.__regression.cause.eventId==='po-scope-request'&&window.__regression.cause.choiceId==='accept-scope'&&!window.__regression.copy.includes('%')`,equals:true}},
 {name:'consequence-regression',action:'screenshot'},
 {name:'close regression before mouse alert check',action:'eval',code:`scene('Workstation').resolveChoice('repair-scope-regression');const s=scene('Workstation');s.interruptions.activate('personal-accidental-mistake',s.workday.snapshot.elapsedGameMs,20);`},
 {name:'open personal alert with mouse',action:'click',x:1100,y:270},
 {name:'mouse opens generic personal decision',action:'expect',expect:{expression:`scene('Workstation').interruptions.snapshot.openInterruption?.id==='personal-accidental-mistake'`,equals:true}},
 {name:'choose response with mouse',action:'click',x:500,y:300},
 {name:'mouse response resolves',action:'expect',expect:{expression:`scene('Workstation').interruptions.snapshot.resolvedOutcomes.some(x=>x.eventId==='personal-accidental-mistake')`,equals:true}},
 {name:'restart clears histories cooldowns and active state',action:'eval',code:`scene('Workstation').scene.restart();`},
 {name:'fresh run state is clean',action:'expect',expect:{expression:`(()=>{const s=scene('Workstation');return s.interruptions.snapshot.active.length===0&&s.interruptions.snapshot.resolvedOutcomes.length===0&&Object.keys(s.interruptions.snapshot.cooldownUntilGameMs).length===0&&s.interruptions.snapshot.concludedConsequenceIds.length===0;})()`,equals:true}}
];
